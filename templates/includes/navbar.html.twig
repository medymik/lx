<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
            <a class="navbar-brand" href="{{ path('index') }}"> {{ render(controller(
                'App\\Controller\\SecurityController::websitename'
            )) }}</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
          
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav ml-auto">
                

                {% if app.user is null %}
               
                <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                      </li>
                      <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                      </li>
                {% else %}


                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <i class="fas fa-shopping-bag"></i> Les produits
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                          <a class="dropdown-item" href="{{ path('products') }}"><i class="fas fa-history"></i>Nos produits</a>
                          <a class="dropdown-item" href="{{ path('products_commander') }}"><i class="fas fa-history"></i> Historiques des produits</a>
          
                        </div>
                    </li>

                      

                          {{ render(controller(
                            'App\\Controller\\ParamController::getCountries'
                        )) }}
                        
                      

                      <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <i class="fas fa-user"></i> {{ app.user.name }}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                          <a class="dropdown-item" href="#">Solde {{app.user.solde }} €</a>
                          <a class="dropdown-item" href="{{ path('demande') }}"><i class="fas fa-history"></i> Retirer</a>
                          <a class="dropdown-item" href="{{ path('changepass') }}"><i class="fas fa-history"></i> Change Password</a>
                          <a class="dropdown-item" href="{{ path('historique') }}"><i class="fas fa-history"></i> Historique de retraits</a>
                          <a class="dropdown-item" href="{{ path('app_logout') }}"><i class="fas fa-sign-out-alt"></i> Déconnexion</a>
                         
                        </div>
                    </li>

                    <li class="nav-item">
                      <a class="nav-link" href="#">Votre Solde {{app.user.solde }} €</a>
                  </li>
                        
                
                {% endif %}

                
                {#
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Dropdown
                  </a>
                  <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="#">Action</a>
                    <a class="dropdown-item" href="#">Another action</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="#">Something else here</a>
                  </div>
                </li>
                #}
              </ul>
            </div>
    </div>
  </nav>